import type { Metadata } from "next";
import FeatureContactsPage from "./Content";

export const metadata: Metadata = {
  title: "CRM for Beatmakers — Manage Contacts | vvault",
  description:
    "Manage your artist, label, and A&R contacts in one place. Track engagement history, organize by tags, and never lose a connection. Built for music producers.",
};

export default FeatureContactsPage;
